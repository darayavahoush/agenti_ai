VITE_API_URL=http://localhost:8000/api/v1
# OAuth client ID from Google Cloud Console (same value backend's
# GOOGLE_CLIENT_ID must be set to -- see backend/app/breathquest_core/config.py).
# Safe to expose client-side; this is a public identifier, not a secret.
VITE_GOOGLE_CLIENT_ID=
